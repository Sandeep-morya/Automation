export default function Pagination() {
    return (
        <div className="pages">
            {/* 
            it should contain 
            * 2 button for prev & Next
            * and between these buttons an heading tag is required to store the current page number

            * on 1st page prev button should be disabled
            * on last page next button should be disabled
            */}
        </div>
    )
}