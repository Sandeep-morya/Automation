export default function Pagination() {
    return (
        <di{/* 
            it should contain 
            * 2 button for Prev & Next
            * Buttons name should be Prev & Next
            * and between these buttons page number is required to store the current page number  (don't use H1)

            * on 1st page prev button should be disabled
            * on last page next button should be disabled
            */}
        </div>
    )
}
